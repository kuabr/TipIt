<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.tipit"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="10" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/appName" >
        <activity
            android:name="de.tipit.activity.TipItActivity"
            android:label="@string/appTitle" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="de.tipit.activity.session.LoginActivity"
            android:label="@string/login" >
        </activity>
        <activity
            android:name="de.tipit.activity.OverviewActivity"
            android:label="@string/appName" >
        </activity>
        <activity android:name="MatchTipActivity" >
        </activity>
        <activity android:name="AdministrationActivity" >
        </activity>
    </application>

</manifest>
